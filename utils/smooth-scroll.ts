"use client"

// This function is no longer needed as we're using a different approach in SmoothScrollInit
export function setupSmoothScroll() {
  // Empty function as we're not using this anymore
  console.log("Smooth scroll setup is now handled directly in SmoothScrollInit component")
}
